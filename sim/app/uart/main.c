#include "peripheral.h"

int main() {
  uart_send('H');
  uart_send('e');
  uart_send('l');
  uart_send('l');
  uart_send('o');
  uart_send(' ');
  uart_send('f');
  uart_send('r');
  uart_send('o');
  uart_send('m');
  uart_send(' ');
  uart_send('U');
  uart_send('A');
  uart_send('R');
  uart_send('T');
  uart_send('!');
}